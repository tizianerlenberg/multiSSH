# Leading slashes matter: bare "proxy"/"agent" would also match the
# cmd/proxy and cmd/agent source directories.
run/
/proxy
/agent
# deploy/build.sh drops the proxy binary here; it was committed once already.
/multissh-proxy
/dist/
